<% @articles.each do |article| %>
  <article>
    <h2><%= article.title %></h2> - <%= link_to '🗑 Delete this article', article_path(article), method: :delete  %>
    <p><%= article.content %></p>
  </article>
<% end %>
